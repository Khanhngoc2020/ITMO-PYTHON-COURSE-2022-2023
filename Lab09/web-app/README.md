# Lab 09 Web App

This Flask application stores portfolio links in PostgreSQL.

Set the database connection through environment variables before running it:

```bash
export DB_USERNAME=postgres
export DB_PASSWORD=your_password
export DB_HOST=localhost
export DB_NAME=postgres
python app.py
```

